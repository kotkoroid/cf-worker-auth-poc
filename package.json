{
  "name": "cf-worker-auth-poc",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "types": "bun run --filter=* types"
  },
  "dependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.4",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9",
    "wrangler": "^4.4.0"
  }
}
